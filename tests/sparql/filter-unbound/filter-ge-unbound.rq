PREFIX : <http://example.org/>

# Test FILTER with GE operator and unbound variables
# Should filter out solutions where variables are unbound
SELECT ?x ?age WHERE {
  ?x a :Person .
  OPTIONAL { ?x :age ?age . }
  FILTER(?age >= 25)
}