PREFIX : <http://example.org/>

# Test FILTER with ROUND function and unbound variables
# Should filter out solutions where math functions with unbound variables return unbound
SELECT ?x ?value ?price WHERE {
  ?x a :Item .
  OPTIONAL { ?x :value ?value . }
  OPTIONAL { ?x :price ?price . }
  FILTER(ROUND(?value) = 11)
}